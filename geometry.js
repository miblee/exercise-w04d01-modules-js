// var geometry = {};
// geometry.area = function(a, b){
//   return a*b;
// };

// module.exports = geometry;

function area(width, height) {
  return width * height;
};

module.exports = {
  area: area
}
